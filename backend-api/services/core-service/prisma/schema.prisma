generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x", "debian-openssl-3.0.x"]
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model User {
  user_id                        Int               @id @default(autoincrement())
  name                           String
  email                          String            @unique
  password_hash                  String
  phone_number                   String?
  profile_picture                String?
  cover_photo                    String?
  gender                         String?           @db.VarChar(30)
  is_active                      Boolean           @default(true)
  is_email_verified              Boolean           @default(false)
  role                           String            @default("USER")
  created_at                     DateTime          @default(now())
  updated_at                     DateTime          @default(now()) @updatedAt
  billing_address                String?
  billing_city                   String?           @db.VarChar(100)
  billing_state                  String?           @db.VarChar(100)
  billing_country                String?           @default("Sri Lanka") @db.VarChar(100)
  billing_postal_code            String?           @db.VarChar(20)
  profile_completed              Boolean?          @default(false)
  date_of_birth                  DateTime?         @db.Date
  emergency_contact_name         String?           @db.VarChar(255)
  emergency_contact_phone        String?           @db.VarChar(20)
  emergency_contact_relationship String?           @db.VarChar(100)
  marketing_emails_enabled       Boolean?          @default(true)
  event_notifications_enabled    Boolean?          @default(true)
  sms_notifications_enabled      Boolean?          @default(true)
  two_factor_secret              String?           @db.VarChar(255)
  two_factor_enabled             Boolean?          @default(false)
  two_factor_backup_codes        Json?
  two_factor_recovery_used       Int?              @default(0)
  last_two_factor_at             DateTime?         @db.Timestamptz(6)
  two_factor_method              String?           @default("app") @db.VarChar(10)
  email_otp_code                 String?           @db.VarChar(6)
  email_otp_expires_at           DateTime?         @db.Timestamp(6)
  organizations                  Organization[]
  search_logs                    Search_Log[]
  payment                        payment[]
  ticket_purchase                ticket_purchase[]

  @@index([billing_country], map: "idx_user_billing_country")
  @@index([phone_number], map: "idx_user_phone_number")
  @@index([profile_completed], map: "idx_user_profile_completed")
  @@index([email_otp_expires_at], map: "idx_user_email_otp_expires_at")
  @@index([is_active, role, created_at(sort: Desc)], map: "idx_user_active_role_created_at")
  @@map("User")
}

model Organization {
  organization_id Int      @id @default(autoincrement())
  user_id         Int
  name            String
  description     String?
  logo_url        String?
  contact_email   String?
  contact_number  String?
  website_url     String?
  created_at      DateTime @default(now())
  updated_at      DateTime @default(now()) @updatedAt
  events          Event[]
  user            User     @relation(fields: [user_id], references: [user_id], onDelete: Cascade)

  @@map("Organization")
}

/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model Event {
  event_id         Int               @id @default(autoincrement())
  organization_id  Int?
  title            String
  description      String?
  category         String?
  venue            String?
  location         String?
  start_time       DateTime
  end_time         DateTime
  capacity         Int?
  cover_image_url  String?
  other_images_url String?
  created_at       DateTime          @default(now())
  status           String            @default("ACTIVE")
  updated_at       DateTime          @default(now()) @updatedAt
  video_url        String?           @default("")
  seat_map         Json?
  ticket_types     Json?
  organization     Organization?     @relation(fields: [organization_id], references: [organization_id])
  payment          payment[]
  ticket_purchase  ticket_purchase[]

  @@index([status, start_time], map: "idx_event_active_start_time")
  @@index([category], map: "idx_event_category")
  @@index([organization_id], map: "idx_event_organization_id")
  @@index([start_time], map: "idx_event_start_time")
  @@index([status], map: "idx_event_status")
  @@map("Event")
}

model Search_Log {
  log_id          Int      @id @default(autoincrement())
  user_id         Int?
  search_query    String
  search_time     DateTime @default(now())
  filters_applied String?
  user            User?    @relation(fields: [user_id], references: [user_id])

  @@map("Search_Log")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model monthly_analytics {
  id              Int       @id
  year            Int
  month           Int
  total_events    Int?      @default(0)
  total_attendees Int?      @default(0)
  total_revenue   Decimal?  @default(0) @db.Decimal(12, 2)
  growth_rate     Decimal?  @default(0) @db.Decimal(5, 2)
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  updated_at      DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([year, month])
}

model payment {
  payment_id      String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id         Int
  event_id        Int
  amount          Decimal           @db.Decimal
  payment_date    DateTime          @default(now()) @db.Timestamp(6)
  status          status            @default(pending)
  payment_method  String?
  transaction_ref String?
  created_at      DateTime          @default(now()) @db.Timestamp(6)
  updated_at      DateTime          @default(now()) @db.Timestamp(6)
  Event           Event             @relation(fields: [event_id], references: [event_id], onDelete: NoAction, onUpdate: NoAction)
  User            User              @relation(fields: [user_id], references: [user_id], onDelete: NoAction, onUpdate: NoAction)
  ticket_purchase ticket_purchase[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ticket_purchase {
  ticket_id     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  event_id      Int
  user_id       Int
  purchase_date DateTime @db.Timestamp(6)
  price         BigInt
  payment_id    String?  @db.Uuid
  seat_id       Int?
  seat_label    String?
  attended      Boolean? @default(false)
  qr_code       String?
  Event         Event    @relation(fields: [event_id], references: [event_id], onDelete: Cascade, onUpdate: NoAction)
  payment       payment? @relation(fields: [payment_id], references: [payment_id], onDelete: Cascade, onUpdate: NoAction)
  User          User     @relation(fields: [user_id], references: [user_id], onDelete: Cascade, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model guest_user {
  id           String   @id @unique @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  name         String   @db.VarChar
  email        String   @db.VarChar
  street       String?  @db.VarChar
  city         String?  @db.VarChar
  postal_code  String?  @db.VarChar
  telephone_no String?  @db.VarChar
  payment_id   String?  @default(dbgenerated("gen_random_uuid()")) @db.Uuid
}

enum status {
  pending
  completed
  failed
  refunded
}
